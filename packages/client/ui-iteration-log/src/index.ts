/** Host loader entry for the iteration-log browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the iteration-log section. */
export function apply(): void {}
